#include <bits/stdc++.h>

void random_syntax() {
    using namespace std;

    // Read in an array
    int n;
    cin >> n;
    vector<int> a(n);
    for (int& ai : a) cin >> a;

    // 64-bit type
    int64_t x;

    // Print to stdout
    cout << x << '\n';

    // Bitset
    std::bitset<20001> bs;

    // Preallocate vector memory
    int n;
    cin >> n;
    vector<int> a;
    a.reserve(n);
    
    // Lambda
    auto lambda = [&](int i, int j) {
        return i < j;
    };
}

int main() {
    ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int T;
    std::cin >> T;
    while (T--) {

    }
}